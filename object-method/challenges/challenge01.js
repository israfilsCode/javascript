const developer = {
    name: "Israfil",
    role: "Full Stack Developer",
    language: "JavaScript",
};

for (const [key, value] of Object.entries(developer)) {
    console.log(key + ": " + value);
}