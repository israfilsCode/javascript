const developer = {
    name: "Israfil",
    role: "Full Stack Developer",
    language: "JavaScript"
};

console.log(Object.keys(developer));

// 🧠 What Object.keys() does
// It returns an array containing the object's own property names (keys).