// Find how many elements are inside of an array

const fruits = ["Apple", "Banana", "Mango"];

const elementsCount = fruits.length;
console.log(elementsCount);


const newElementsCount = fruits.push("Orange");
console.log(newElementsCount);


for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i]);
}