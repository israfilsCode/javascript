const developer = {
    name: "Israfil",
    age: 20,
    role: "Full Stack Developer",
    language: "JavaScript"
};

const {name, age, role, language} = developer;

console.log(name);
console.log(age);
console.log(role);
console.log(language);

// This is especially useful when working with React props, API responses, Node.js, and configuration objects